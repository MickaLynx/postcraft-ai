import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Algorithm Tracker — Decode Platform Algorithms & Optimize Reach | PostCraft AI',
  description: 'Track social media algorithm changes in real-time. Get ranking factors, content rules, penalty zones, and optimization tips for Instagram, TikTok, LinkedIn, Twitter/X, and more.',
};

export default function AlgorithmTrackerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
