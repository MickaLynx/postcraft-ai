import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Platform Migration Guide — AI Content Strategy — PostCraft AI',
  description: 'Plan your social media platform migration with AI. Week-by-week roadmaps, content adaptation strategies, and audience retention tactics.',
};

export default function PlatformMigrationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
