import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Retention Playbook Generator — AI Growth Strategy — PostCraft AI',
  description: 'Build data-driven retention playbooks with AI. Onboarding flows, engagement loops, and re-activation campaigns for any product.',
};

export default function RetentionPlaybookLayout({ children }: { children: React.ReactNode }) {
  return children;
}
