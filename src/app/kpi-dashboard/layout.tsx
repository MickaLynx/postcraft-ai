import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'KPI Dashboard Builder — AI Social Media Analytics — PostCraft AI',
  description: 'Design custom social media KPI dashboards with AI. Track the metrics that matter, benchmark performance, and generate executive reports.',
};

export default function KpiDashboardLayout({ children }: { children: React.ReactNode }) {
  return children;
}
