import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'UGC Script Generator — AI User-Generated Content — PostCraft AI',
  description: 'Generate authentic UGC scripts for any platform. Hooks, scenes, transitions, and CTAs designed for maximum creator engagement.',
};

export default function UgcScriptsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
