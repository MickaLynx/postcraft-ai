import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'AI Storytelling Engine — Transform Brand Stories | PostCraft AI',
  description: 'Generate compelling multi-platform narratives with AI. Story blueprints, narrative arcs, platform adaptations, engagement predictions, and content variants.',
};
export default function StorytellingEngineLayout({ children }: { children: React.ReactNode }) { return children; }
