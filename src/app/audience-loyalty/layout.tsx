import type { Metadata } from 'next';
export const metadata: Metadata = {
  title: 'Audience Loyalty Tracker — Measure & Strengthen Loyalty | PostCraft AI',
  description: 'Track audience loyalty, segment superfans, detect churn risks, and get personalized retention playbooks. Turn followers into lifelong brand advocates.',
};
export default function AudienceLoyaltyLayout({ children }: { children: React.ReactNode }) { return children; }
