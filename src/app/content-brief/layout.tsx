import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Content Brief Generator — AI Content Planning | PostCraft AI',
  description: 'Generate comprehensive content briefs in seconds. SEO guidance, creative direction, distribution plans, and success metrics — all AI-powered.',
};

export default function ContentBriefLayout({ children }: { children: React.ReactNode }) {
  return children;
}
