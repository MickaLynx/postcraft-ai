import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Topic Miner — Discover Untapped Content Opportunities | PostCraft AI',
  description: 'Mine untapped content topics from adjacent industries, emerging narratives, and content gaps. Find golden topics with high demand and low competition powered by AI.',
};

export default function TopicMinerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
