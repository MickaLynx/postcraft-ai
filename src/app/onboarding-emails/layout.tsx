import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Onboarding Email Generator — AI Email Sequences — PostCraft AI',
  description: 'Generate high-converting onboarding email sequences with AI. Welcome flows, activation emails, and conversion sequences.',
};

export default function OnboardingEmailsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
