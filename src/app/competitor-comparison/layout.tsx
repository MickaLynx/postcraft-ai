import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Competitor Comparison Page Generator — AI Marketing — PostCraft AI',
  description: 'Create compelling competitor comparison pages with AI. Feature matrices, positioning copy, and SEO-optimized alternative pages.',
};

export default function CompetitorComparisonLayout({ children }: { children: React.ReactNode }) {
  return children;
}
